[package]
name = "nxuskit-examples-clips-wire"
version = "0.1.0"
edition = "2024"
publish = false
description = "Shared ClipsInput/ClipsOutput wire types for nxusKit-examples (provider chat JSON)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
